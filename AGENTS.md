# 투자리서치 프로젝트 공통 지침

Codex는 이 파일을 읽고, CLAUDE.md는 이 파일을 참조한다. 현행 운영 기준은 이 문서, PRINCIPLES.md, README.md이며 이전 설계 문서는 역사적 맥락이다.

## 목적과 범위

시장이 아직 충분히 알아채지 못한 이익 증가를 남들보다 먼저 찾는 발굴 엔진이다. 매일 자동 수집에서 이익 전망을 크게 바꿀 수 있는 특이값을 찾아 후보로 올리고, 근거가 강한 후보는 '추적 추천'한다. 사용자가 추적을 선택한 종목은 종목별 페이지에서 숫자와 판단을 갱신한다. 발견의 양보다 좋은 후보 하나가 중요하다.

- 산업 제한 없이 해외(미국·글로벌) 기업을 우선한다. 국내는 SK하이닉스급 독보적 경우만 본다. 대형 고객은 수요 근거로 보존하되 신규 투자 후보와 구분한다.
- 후보 순위는 산업이 아니라 (1) 회사 규모 대비 이익 변화 크기, (2) 시장 예상치의 미반영, (3) 공급 부족·가격 결정력 같은 지속 근거로 정한다. 병목 확산형·사이클 리비전형은 대표 유형이다.
- 여러 회사에서 같은 방향의 신호가 동시에 나오면 테마로 묶어 올린다.
- 추적 추천은 추적할 가치에 대한 판단이며 매수 추천·목표주가·상승 확률이 아니다. 이유, 근거 원문, 틀렸다고 볼 조건을 함께 붙인다.
- 주가가 크게 오른 사례를 사후 분석해 당시 잡을 수 있었던 신호를 찾고 발굴 조건·출처를 보강한다. 새 조건은 등록일 이후 결과로만 평가한다.
- 심층 검증은 추적 종목에 한해 수행한다. 발굴의 폭과 후보 품질이 우선이며 소수 종목 검증에 작업이 쏠리지 않게 한다.
- 결과는 사용자가 읽을 수 있는 한국어로 쓴다. 회사가 무엇으로 돈을 버는지와 전문용어를 먼저 쉽게 설명한다.
- 현재 추적 종목: ALAB, CRDO, MRVL.

## 연구 규칙

- 사실, 회사 가이던스, 시장 컨센서스, 연구자의 해석을 분리한다. 원문 URL·인용·발표일·관측일·대상 기간을 기록한다.
- 사업 단계, 근거 수준, 검토 상태를 별도로 판단한다. 기사 하나로 시계열 검증·중기 승격을 하지 않는다.
- 같은 대상 회계기간·출처·통화·단위·회계기준의 EPS 추정치를 비교한다. 실적 성장과 컨센서스 상향은 다르다.
- 같은 날짜 구간의 EPS와 가격으로 계산한 PER 변화는 참고 지표이며 저평가를 입증하지 않는다.
- 병목 가설에는 공급 제약, 수요 증거, 수혜 연결, 해소 조건과 독립적인 반증 근거를 붙인다. 문서 수를 독립 근거 수로 간주하지 않는다.
- 미확인 값과 접근 실패를 명시한다. 추정치나 과거 관측값을 만들어 넣지 않는다.
- 연구 결과는 결론·근거·한계·다음 점검을 설명한다. 자동 매매·목표주가·포지션 비중은 이 시스템의 범위 밖이다.

## 데이터와 개발

- 컬럼·enum은 config/schema.json, 운영 한도는 config/research_policy.json이 단일 원천이다.
- Python 3.11 이상, 표준 라이브러리로 실행한다. Gemini·SEC·RSS·FMP·Telegram 외부 연결은 README에 명시한다.
- CSV는 UTF-8-SIG. 변경은 add_entry.process를 사용한다. 마이그레이션은 백업을 먼저 보관한다.
- 투자 판단 갱신은 이유와 이전/이후 스냅샷을 review_history에 함께 기록한다. 기업+가설을 기준으로 중복 등록을 막는다.
- example/quarantine은 운영 보드·알림에서 제외한다. legacy는 재검증 대상이며 자동 추천에 사용하지 않는다.
- 외부 API 오류는 상태 원장에 남기고 실패를 신호로 변환하지 않는다. 비밀키·토큰이 포함된 URL을 로그에 남기지 않는다.
- CSV 쓰기는 단일 작성자를 전제로 한다. CI는 research-state-writer concurrency를 사용한다. 로컬 수동 쓰기와 CI를 동시에 실행하지 않는다.
- 복구 저널이 있으면 먼저 복구한다. 상태 파일 손상을 빈 상태로 바꿔 중복 전송하지 않는다.
- 관련 동작을 테스트하고 실제 외부 전송 없이 dry-run으로 보고서를 확인한다.

## 작업 권한

사용자가 구현·개선을 지시하면 필요한 가역적인 코드·문서 수정과 검증을 완료한다. 연구 원장 변경은 사용자가 승인한 범위에서 add_entry를 통해 수행한다. 별도 요청 없는 외부 메시지 전송·자동 매매는 수행하지 않는다. 하위 에이전트는 명시적인 요청이 있을 때만 사용한다.

## 작업자 인수인계

사용자 지정 역할(2026-09-25): Codex는 기획·설계·검토·구현 지시를 중심으로 맡고 Claude 등 구현 담당자가 실행한다. Codex에게 별도 구현 요청이 있으면 그 요청을 따른다. 지시서는 목적, 우선순위, 수정 파일, 입력·출력/데이터 규칙, 예외 처리, 제외 범위, 테스트, 완료 기준, 인수인계 증거를 포함한다. 미정 사항을 구현 담당자가 추측해야 하는 추상적 지시로 남기지 않는다. 문서상 설계와 실제 구현·운영 검증 완료를 구분한다.

Codex와 Claude가 번갈아 작업한다. 동시에 같은 체크아웃·원장에 쓰지 않는다. 작업을 멈추거나 끝낼 때는 STATUS.md 맨 위 인수인계 칸의 마지막 작업자, 진행 중인 쓰기, 다음 할 일, 미해결 항목을 갱신한다. 작업을 시작할 때는 이 칸과 git status를 먼저 확인한다.

## 향후 역할 설계

업종 지표 정의, 병목 탐색, EPS 관측, 반증 점검, 복기는 업무 역할이다. 현재 자동 에이전트 팀을 운영한다는 뜻은 아니다. 발굴 신호의 폭과 후보 품질 개선을 우선한다.
