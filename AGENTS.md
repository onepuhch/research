# investment-research-system — 프로젝트 지침 (Claude Code + Codex 공용)

> 이 파일이 리서치/개발 지침의 **단일 원본**이다.
> - Codex 는 `AGENTS.md` 를 직접 읽는다.
> - Claude Code 는 `CLAUDE.md` 가 이 파일을 `@import` 한다.
> - 따라서 지침을 바꿀 때는 **이 파일만** 고치면 두 도구에 반영된다.

---

## 1. 이 프로젝트의 정체 (가장 중요)
**이 시스템의 진짜 목적은 "투자 후보 조기 발굴"이다.** 파일·CSV 관리가 목적이 아니다.
메인 테마의 **병목이 다음 어디로 옮겨가는지**, 그 병목에 가장 직접 노출된 **순수 플레이어가 누구인지**를 *시장이 다 알아차리기 전에* 찾고 추적한다.
- 예: AI 데이터센터 = GPU/HBM → NAND/eSSD · 전력 · 냉각 · 광통신 · 네트워크 · 인프라로 병목 확산. 시장이 다 안 뒤 따라가지 않고, 컨콜·CAPEX·수주·ASP·리드타임·EPS 상향·고객 코멘트·장기계약·선급금·재고로 **초기에 감지**한다.
- 시스템이 해야 할 7가지: ①밸류체인 분해 ②병목 확산 구간 탐지 ③순수 플레이어 후보 기록 ④EPS·ASP·수주·CAPEX·공급망·고객 코멘트로 검증 ⑤단계 분류(관찰~제외) ⑥주기적 강화/약화 업데이트 ⑦복기로 발굴 정확도 향상.
- **단기 매매 추천기·뉴스 요약기가 아니다.** 투자 기간은 고정하지 않는다.
- 투자 철학 전문은 [PRINCIPLES.md](PRINCIPLES.md), 발굴 절차는 아래 §9, 사용법은 [README.md](README.md).

## 2. 리서치 핵심 원칙
- **해외(미국·글로벌) 기업·리포트를 우선한다.** 국내는 사용자가 직접 보며, 기회·데이터가 해외에 더 많음. 한국 종목은 SK하이닉스처럼 독보적인 경우만 예외. (해외 자료는 핵심을 한국어로 풀어 전달)
- **forward(미래) 밸류로 본다.** trailing PER이 아니라 *forward EPS·forward PER*, 그리고 'forward EPS 상향률 vs 주가 상승률(동일 기간)'로 저평가를 판단한다.
- 개별 종목보다 **업종과 밸류체인**을 먼저 본다.
- "좋은 회사"보다 **"시장이 아직 충분히 반영하지 않은 이익 변화"**를 찾는다.
- 극초기 턴어라운드 베팅보다, **선행지표 개선 + EPS 상향이 시작되는 초기후반~중기초입**을 선호한다.
- 보유 기간은 미리 정하지 않는다. 매수 논리가 숫자로 강화되는 동안 보유, 핵심 지표 둔화 시 축소/종료 검토.
- PER 이 낮아 보여도 **EPS 상향이 멈추고 핵심 지표가 둔화되면 위험**하다.
- 주가가 많이 올랐어도 **EPS 상향률 > 주가 상승률 + 선행지표 유지**면 검토 가능하다.
- 아이디어별로 **예상 시간축 · 핵심 촉매 · 보유 조건 · 종료 조건**을 구분한다.

## 3. 운영 원칙 (프레임은 고정된 정답표가 아니다)
- 기존 지침/체크리스트는 정답표가 아니라 **리서치의 출발점**이다.
- 분석 시작 시 먼저 정의한다: **"이번 업종/기업에서 이익을 실제로 움직이는 핵심 변수는 무엇인가?"**
- 기존 지침에 없는 변수라도 중요하면 **추가하고, 왜 중요한지 설명**한다.
- 업종·사이클·기업 특성에 따라 핵심 지표와 우선순위는 달라질 수 있다.
- 기존 프레임으로 설명되지 않는 변수, 시장이 덜 보는 변수를 **반드시 찾는다.**
- 분석 중 더 나은 프레임이나 기존 지침의 한계가 보이면 **마지막에 "지침 업데이트 제안"**을 제시한다.

## 4. 답변 형식 (리서치 질문에 답할 때)
1. **결론을 먼저** 말한다.
2. 현재 위치가 **관찰 / 초기 / 초기후반 / 중기 / 후기 / 제외** 중 어디인지, [PRINCIPLES.md 단계 루브릭](PRINCIPLES.md)의 **숫자 기준**으로 판단한다. 업종 고유 기준이 있으면 그 숫자를 함께 제시한다.
3. **아이디어 유형**을 구분한다. (사이클 리비전형 · 병목 확산형 · 이벤트 촉매형 · 구조적 성장형 · 가치 재평가형)
4. **긍정 요인 · 리스크 · 핵심 모니터링 지표 · 매수/보유/축소 기준**을 구분해 적는다.
5. **"내 분석이 틀릴 수 있는 이유"**를 포함한다.
6. 최신 정보가 필요하면 **웹 검색으로 확인하고 출처**를 단다.
7. 확실하지 않은 내용은 **확실하지 않다고** 말한다.
8. 마지막에 **"지금 진입 가능 / 대기 / 보유 / 축소"** 관점으로 정리한다.

## 5. 코드베이스 작업 규칙 (스크립트/데이터를 고칠 때)
- **단일 진실원천**: 테이블/컬럼/허용값(enum)은 `config/schema.json` 에서만 바꾼다. 코드 하드코딩 금지.
- CSV 는 **UTF-8-SIG** 로 쓴다(엑셀/구글시트 한글 깨짐 방지).
- 현재는 **표준 라이브러리만** 사용(외부 패키지·외부 API 미연동). 추가 시 README 에 명시.
- 스크립트 역할: `add_entry`(추가/갱신) · `export_tsv`(시트 복붙) · `gen_report`(리포트/공유 요약) · `create_templates`(템플릿).
- 테이블 성격을 지킨다: master=upsert, log=append, tracked=idea_id 갱신. **같은 업종/아이디어가 중복 행으로 늘어나면 안 된다.**

## 6. 작업 방식
- 기본 **위임형**: 방향이 분명하면 실행까지 끝내고 결과로 보고.
- 단, "계획/설계만" 요청 시에는 실행 전에 **계획을 함께 검토**하고 승인받는다.
- 실행 예시·명령어는 [README.md](README.md) 참조 (Windows PowerShell 기준).

---

## 7. 미래 에이전트 역할 (설계만 — 아직 구현하지 않음)
> 지금은 **실행 기능을 붙이지 않는다.** 데이터가 쌓이고 반복 패턴이 드러난 뒤
> (스킬 → 서브에이전트 순으로) 구현한다. 아래는 그때를 위한 **역할 명세**다.
> Claude Code 서브에이전트(`.claude/agents/`)와 Codex 에이전트 양쪽이 이 명세에서 파생된다.

### 모든 에이전트 공통 규칙
- **초안/제안만 생성한다.** CSV를 직접 수정하지 않는다 — 사람이 검토한 뒤 `add_entry` 로만 반영.
- 위 "3. 운영 원칙"과 "4. 답변 형식"을 따른다(결론 먼저 · 출처 명시 · 불확실성 표기 · "틀릴 수 있는 이유" 포함).
- **매매 추천 · 목표주가 · 포지션 비중을 내지 않는다.** (이 시스템은 리서치 도구이며, 비중은 분리된 실행 결정)
- 단계 판정은 [PRINCIPLES.md] 루브릭의 **숫자 기준**을 쓰고, 업종 고유 기준은 `다음 단계 트리거`로 제시한다.

### 7-1. Sector Indicator Agent
- **목적**: 한 업종에서 "이익을 실제로 움직이는 핵심 변수"와 선행/후행지표·위험신호·출처를 정의·갱신.
- **입력**: 업종명(+ 있으면 sector_id), 최신 산업 데이터(웹).
- **출력**: `industry_indicators` upsert용 JSON (업종 키). → `add_entry`
- **절대 금지**: 종목 단위 매수 의견, 출처 없는 수치 단정.
- **판단 기준**: "이 업종에서 이익이 실제로 움직이는 변수는?"을 먼저 정의. 기존 지침에 없어도 중요하면 추가하고 이유를 적는다.

### 7-2. Bottleneck Scout Agent
- **목적**: 밸류체인에서 수요>공급 병목·시장이 덜 본 이익 레버리지 지점 탐색.
- **입력**: 메인 테마/밸류체인, 최신 수급·CAPEX·리드타임 데이터(웹).
- **출력**: `bottleneck_log` append용 JSON. → `add_entry`
- **절대 금지**: 병목 근거 없이 "순수 플레이어"를 종목 추천처럼 단정.
- **판단 기준**: "왜 시장이 아직 덜 보는가"를 반드시 명시. 리드타임·인증·CAPA 같은 구조적 병목 우선.

### 7-3. EPS Revision Agent
- **목적**: 활성 아이디어의 EPS 컨센서스 리비전(방향·연속성·폭)을 추적해 단계 이동을 제안.
- **입력**: 종목/업종 + `investment_review_log`의 모니터링 지표, 최신 컨센서스(웹).
- **출력**: 리비전 요약 + 단계 이동·`근거 강도` 갱신 제안 → `investment_review_log` 갱신 JSON 초안(idea_id 지정). → `add_entry`
- **절대 금지**: 단일 시점 추정치로 추세 단정. EPS 상향률 vs 주가 상승률을 **다른 기간으로** 비교.
- **판단 기준**: 루브릭(상향 개시→연속→확대→둔화). 상향률 vs 주가는 동일 기간(기본 3개월).

### 7-4. Risk Check Agent (반증)
- **목적**: 아이디어를 **공격**한다. 종료 조건(정량) 발동 여부·리스크 현실화·"틀릴 수 있는 이유"를 독립 점검.
- **입력**: idea_id 1건 + 최신 데이터.
- **출력**: 위험 플래그(종료/축소 트리거 근접도) + 반증 논리. 필요 시 `현재 단계` 하향/제외 제안 JSON 초안.
- **절대 금지**: 강세 논리 옹호(역할상 반증만 한다). 원 아이디어 논리를 그대로 반복.
- **판단 기준**: 종료 조건(정량)·위험 신호를 숫자로 대조. 트리거 충족 시 명확히 "발동"이라고 말한다.

### 7-5. Review Agent (복기)
- **목적**: 종료/후기 아이디어의 결과를 복기하고 아이디어 유형별 적중률을 누적.
- **입력**: idea_id(제외/후기) + 실제 결과.
- **출력**: `investment_review_log` 갱신 JSON(이후 결과·맞은 점·틀린 점·프레임 수정) + 유형별 적중 요약. → `add_entry`
- **절대 금지**: 사전 종료조건과 대조 없는 결과론적 합리화.
- **판단 기준**: 사전에 적은 보유/종료 조건 대비 실제. 프레임 한계 발견 시 "지침 업데이트 제안"으로 마무리.

---

## 8. 미래 데이터 확장 — metric_log (설계만, 미구현)
EPS 컨센서스/가이던스 등 선행지표의 **변화(Δ)를 시계열로** 기록하는 테이블. 기사 검색(후행) 대신
리비전을 숫자로 추적해 단계 루브릭에 투입한다.
- **전 시장을 미리 쌓지 않는다.** 아이디어/병목 후보 + **관찰 후보**(상향을 목격해 가볍게 적어둔 것)만 기록.
- 각 줄에 **이전값→현재값**을 담아 baseline 문제를 푼다. 같은 FY끼리만 비교(지표명에 연도 박기).
- **발굴 퍼널**: 외부 스크린(네이버 금융 · 해외 IB 리포트 · 한경 컨센서스)에서 상향을 보면 관찰 후보로 기록 → `gen_report metric`(발굴 보드)에서 **반복 상향**이 쌓인 것을 정식 아이디어로 **승격**.
- 전체 구현 브리프: [docs/metric_log_design.md](docs/metric_log_design.md) — **구현은 Codex 담당.**

---

## 9. 발굴 RUN 절차 (온디맨드 — 지금 쓰는 핵심 기능)
테마를 주면, 병목을 따라가 *시장이 아직 덜 연결한* 순수 플레이어를 **cold하게**(이름을 미리 주지 않아도) 끌어낸다. 크롤러·스케줄러 없이 웹검색+추론으로 수행한다. (= §7-2 Bottleneck Scout 의 실행 절차)

1. 테마의 밸류체인을 단계로 분해 (예: AI DC = 연산 → 메모리 → 스토리지 → 전력 → 냉각 → 네트워크/광 → 인프라).
2. 각 단계의 '특이값' 신호 수집: 컨콜 · CAPEX · 수주 · ASP/가격 · 리드타임 · EPS 상향 · 고객 코멘트 · 장기계약 · 선급금 · 재고.
3. **이미 시장이 본 칸**(메가캡·급등주)은 제외하고, **병목이 새로 옮겨가는/덜 본 칸**을 고른다.
4. 그 칸의 **순수 플레이어 후보를 surface** 한다 — 신호에서 *발견*하는 것이지 답을 미리 넣지 않는다.
5. 각 후보를 단계(관찰~제외)로 분류 + '확인할 지표'를 단다.
6. `bottleneck_log` 기록 → 강화되면 `investment_review_log` 승격 → `metric_log` 추적.

**반(反)순환 원칙: 이름을 미리 알고 검색하면 순환이다. 항상 신호 → 후보 순서. 검증 전엔 매수 신호 아님.**

이 RUN을 매일 자동화(수집→추출→텔레그램 푸시)하는 전체 구현 브리프: [docs/discovery_engine_design.md](docs/discovery_engine_design.md) — **무료 스택, Codex 담당.**

### 진행 중 구현 브리프 (Codex 담당)
- **Phase 2 텔레그램 푸시**: [docs/notify_design.md](docs/notify_design.md) — 티어 A/B 신호만 push, 중복방지, 레딧 제외.

> **역할 분담(2026-06-07~)**: 설계·검토는 Claude, **코딩은 Codex**. Codex는 위 브리프 문서를 단일 스펙으로 구현하고, §7 공통 규칙(초안만·출처·불확실성)과 §5 코드 규칙(schema 단일원천·UTF-8-SIG·표준라이브러리)을 지킨다.
