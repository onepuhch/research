# J 인수인계 (Claude, 2026-09-27 01:10 KST 무렵)

지시서: [J 지시서](j_ops_and_local_model_spec_2026-09-27.md). 구현 완료와 실제 운영 확인을 나눠 적는다.

## 완료 표

| 항목 | 상태 | 커밋 | 증거 |
|---|---|---|---|
| I2 판정 | 반영(재구현 없음). 생성 시각 ≠ 공시일·원문 확인일 원칙 유지 | - | Codex 수용 |
| J1 준비(감사 사본) | **구현·CI 완료, 운영 미확인** | 0a1ede0 | CI 36251807028 성공, 테스트 6개. 첫 운영 자료는 9/27 정상 실행 아티팩트 `research-reports-<run_id>`의 `context_audit/2026-09-27/` |
| I3-2/J1 평가 | **운영 대기** | - | 9/27 09:17 KST 정상 실행 뒤 진행 |
| J2 타이머 | 원인 확정(PC 전원 꺼짐). 보완 없음. 자연 실행 확인 대기 | d882731 | [진단](j_timer_diagnosis_2026-09-27.md) |
| J3 로컬 모델 | **실제 추론 완료, 판정 '현재 보류'**, Gemini 비교 미판정 | 320e37d, 5149743 | [평가](j_local_model_evaluation_2026-09-27.md) |
| 검증기 v5 | 구현·테스트 완료(dev 사례 AMCX-B의 GAAP 오표기 수정). 운영은 9/27 실행이 처음 | 5149743 | 361 통과 + 알려진 누락 1(expectedFailure) |
| 운영 불변 | 로컬 시험 중 추가 API 호출·발송·등록·운영 원장 쓰기 없음 | - | git status, 도구 테스트 |

## 커밋별

- e9e5b92: Codex의 J 지시서와 STATUS를 커밋.
- 0a1ede0 J1
  - 대상: `candidate_context.run_drafts`가 시도마다 `reports/generated/context_audit/<KST일>/<attempt>.json`을 남긴다(512KiB 상한, 원자 저장).
  - 기록 내용: 입력 문서 해시, 블록, 정확한 프롬프트와 해시, 검증 전 answer 전체, 원응답 텍스트(`extract.call_gemini_prompt(raw=…)`), 검증 결과, 실패 종류와 HTTP 코드만, 예산 전후, 체크아웃된 코드 SHA(.git 직접 읽음).
  - 저장이 실패해도 초안 처리는 계속되고, 요약에 `audit_write_failed`가 남는다. 요청 수·예산·카드·알림 동작은 바뀌지 않는다.
  - 테스트 격리: RunFixture가 audit_dir을 임시 폴더로 돌린다. 처음 전체 테스트를 돌렸을 때 저장소 `reports/generated`(git 제외)에 테스트용 가짜 후보 파일 8개가 생겼는데, 확인 후 지웠다.
- d882731 J2
  - 문서: 진단 문서.
  - 정정: 장애 기록과 I 인수인계의 "9/26 dispatch 없음"을 고쳤다.
  - README: 09:23 명령 트리거가 없는 이유와 대체 예약 지연(3~5시간)을 적었다.
- 320e37d J3
  - 도구: `scripts/local_context_benchmark.py`(prepare/run/report).
  - 입력: `tests/fixtures/local_context_benchmark/manifest.json`(10문서 해시, 20사례, dev 4, stability 4)과 `review_labels.json`(모델 출력 보기 전 작성).
  - 테스트: 가짜 서버 12개.
- 5149743
  - 검증기 context-check-v5(GAAP 기준), 일간 context 단계 context-v5.
  - NBR-B 알려진 누락을 expectedFailure 테스트로 남김.
  - 채점에 GAAP 불일치 검사 추가.
  - `tests/live_context_evaluation.py`(I3-2 집계, 읽기 전용).

## 로컬 환경 (저장소 밖)

- Ollama 0.34.4를 winget으로 설치했다(`%LOCALAPPDATA%\Programs\Ollama`).
- 사용자 환경변수 4개를 추가했다: OLLAMA_NO_CLOUD=1, OLLAMA_NUM_PARALLEL=1, OLLAMA_MAX_LOADED_MODELS=1, OLLAMA_HOST=127.0.0.1:11434.
- 모델: `C:\Users\wls15\.ollama\models`의 qwen3.5:9b 한 개(6.6GB).
- 설치 직후 뜬 트레이 앱과 서버는 설정을 적용하려고 종료했다. 대신 `ollama serve`를 숨김 모드로 실행 중이며, 127.0.0.1에서만 수신한다. 모델은 10분 뒤 자동으로 내려간다.
- 사용자가 설치 창에서 Ollama 계정에 로그인했다. 시험에는 쓰지 않았다.
- 설치 프로그램이 로그인 시 트레이 앱이 자동으로 시작되도록 등록했을 수 있다. 원치 않으면 트레이 메뉴 Quit이나 시작 프로그램에서 끄면 된다. 이번 작업에서 이 설정은 바꾸지 않았다.

## 재개 명령

- 로컬 시험 보고서 재생성(네트워크 없음):

  ```text
  python scripts/local_context_benchmark.py report --experiment reports/generated/local_model/j3-55774e6d13-context-check-v4 --labels tests/fixtures/local_context_benchmark/review_labels.json --output reports/generated/local_model/j3-55774e6d13-context-check-v4/report
  ```

- I3-2(정상 실행 뒤):
  1. `git pull`
  2. 해당 run의 아티팩트 `research-reports-<run_id>`를 받아 `context_audit/2026-09-27/`을 확인한다.
  3. 집계: `python tests/live_context_evaluation.py 2026-09-27 --audit <아티팩트>/context_audit/2026-09-27`
  4. 최대 6개 기업을 저장된 전체 원문과 대조한다.
  5. 결과는 `docs/i3_live_evaluation_2026-09-27.md`에 남긴다.
  6. 감사 사본의 prompt_sha256이 J3 A군 사례의 prompt_sha256과 같으면 paired로 J3 표에 합친다.
- J2: dispatch.log 마지막 줄과 두 예약 작업의 LastRunTime/LastTaskResult를 읽고, GitHub workflow_dispatch run 생성 시각과 대조한다.

## 미해결·Codex 확인 요청

1. 검증기 과잉 거부 후보 2건(dev): 글머리 기호 `◦`/`•` 차이, 'bookings'가 'backlog' 수치의 가장 가까운 지표로 잡힘. 완화 방향이라 정상 실행 전에는 고치지 않았다. 수정 여부를 결정해 달라.
2. NBR-B 알려진 누락(매각 사업부 수치를 회사 수치로 표기). 규칙 설계가 필요하다(예: "from <사업 이름>, which was sold" 형태). eval에서 나온 사례이므로 새 규칙은 다른 자료로 검증해야 한다.
3. 로컬 모델 후속 방향: 좁은 범위 재시험을 할지, 보류를 유지할지.
4. 계속 미확인: 원격 예약→발송→영수증, 실제 /track, 9/28 주간 단계, 운영에서의 partial/실패 카드.
